import React, { useEffect } from 'react'


export const About = () => {
                                       //We want it to run just once, so passing and empty array
  return (
    <div>
      This is About Page
    </div>
  )
}
